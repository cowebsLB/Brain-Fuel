# Worklog - 18-05-2026

## Project Initialization and Scaffolding

- Started from a nearly empty repository containing only a minimal README.
- Explored the provided mockup/reference directory `stitch_brain_fuel_digital_menu` and identified available variants, assets, and generated HTML.
- Selected `brain_fuel_digital_menu` as the primary reference direction and extracted visual system cues from `brain_fuel_chalkboard_system/DESIGN.md`.
- Initialized a Node + React project with Vite at repository root.
- Installed project dependencies and set up a local `postcss.config.js` to avoid conflicts with external/global PostCSS config.

## Core Frontend Implementation

- Replaced starter template with a structured React app:
  - `src/App.jsx`
  - `src/components/*`
  - `src/data/menuData.js`
  - `src/styles/*`
- Built the menu as data-driven rendering (single source of truth for categories and items).
- Implemented semantic sections for:
  - Header
  - Hero
  - Category cards
  - Parallax separators
  - Footer
  - Mobile category nav
  - Floating WhatsApp action
- Ported and normalized menu content from mockup data.

## Visual System and Styling

- Implemented chalkboard-inspired design tokens and CSS architecture:
  - color variables
  - typography scales
  - spacing and layout constraints
  - component-level styling
- Reworked layout to feel more polished and intentional:
  - layered backgrounds
  - framed cards
  - improved hierarchy/spacing
  - responsive behavior for mobile and desktop
- Improved bottom nav, floating action button placement, and footer card styling.

## Motion and Interaction

- Added interaction system:
  - page reveal animations
  - card hover/focus states
  - CTA micro-interactions
  - mobile active-tab indicator
- Added reduced-motion accessibility fallback for animation and parallax behaviors.
- Added hero atmospheric steam accent effect.

## Parallax Evolution

- Implemented initial section separator parallax.
- Iteratively reworked separators to true **background parallax** strips instead of foreground image panels.
- Converted separators to full-bleed background strips with scroll-driven background position.
- Tuned overlay darkness for readability while preserving image visibility.
- Set desktop `background-attachment: fixed` enhancement and reduced-motion fallbacks.

## Social, Contact, and Action Links

- Added/updated action links:
  - WhatsApp deep link (international format)
  - Google Maps link
  - Instagram link
  - TikTok link
  - direct call (`tel:`)
- Moved action controls into footer icon cluster.
- Converted emoji actions to icon library implementation (`react-icons`).
- Added analytics event hooks for CTA clicks via `src/utils/analytics.js` with support for `gtag`, `plausible`, and `fathom` when present.

## Intro Animation System (Major Iteration)

- Implemented branded opening intro (`OpeningIntro`) with skip control, session gating, and reduced-motion disable behavior.
- Added force preview mode (`?intro=1`) for repeated testing.
- Iteratively debugged and refined timing/behavior based on visual feedback:
  - center alignment issues
  - layer overlap artifacts
  - split line synchronization
  - split sequence pacing
  - logo visibility and z-index conflicts
- Refactored intro from overlapping-delay approach to phased state-driven sequence to avoid triple-logo artifacts.
- Final intro behavior includes:
  - logo phase
  - split phase
  - synchronized split line/panel movement
  - overlay fade-out
- Retimed intro duration multiple times and finalized current runtime.

## Footer/Content Refinements

- Updated footer content and hours formatting.
- Removed duplicated/unneeded plain text contact lines per request.
- Improved footer visual structure into compact “Visit/Follow/Message” style with cleaner spacing.

## SEO, Metadata, and App Shell

- Updated `index.html` metadata:
  - title
  - description
  - OpenGraph tags
  - theme-color
- Added and wired favicon set generated from logo asset:
  - `public/favicon.png`
  - `public/favicon-32.png`
  - `public/favicon-16.png`

## Asset Optimization

- Generated optimized image formats (`avif`, `webp`) from source assets using `sharp`.
- Updated separator/background usage to prefer modern image formats with fallbacks.

## Documentation Work

- Added required project-level documentation:
  - `index.md`
  - `docs/architecture.md`
  - `docs/decisions.md`
  - `docs/features.md`
  - `docs/limitations.md`
  - `docs/future-improvements.md`
  - `docs/changelog.md`
- Updated README scaffold from Vite default to project-relevant state as part of repository changes.
- Added this detailed dated worklog file.

## Build/Verification History

- Repeatedly validated changes using `npm run build` after each major round.
- Resolved build blockers encountered during development:
  - PostCSS configuration conflict
  - CSS syntax error from unclosed block during intro refactor
- Final build status at commit time: successful.

## Repo/Operational Notes

- Work completed on branch: `main`.
- Changes include source, assets, docs, configuration, and lockfile updates.
- Deployment target noted by owner: GitHub Actions + GitHub Pages workflow (to be configured/pushed separately if requested).