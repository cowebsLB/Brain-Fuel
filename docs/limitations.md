# Known Limitations

- Parallax behavior on low-end mobile devices may still feel less smooth than desktop under heavy CPU load.
- CTA links are currently placeholders (`#`) and need real external destinations.
- Iconography uses emoji for lightweight portability and can be replaced with a dedicated icon set.
- No automated test suite is included yet; verification is currently manual and build-based.